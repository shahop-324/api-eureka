import React from "react";
import styled from "styled-components";

const SpeakerMagicLinkDestination = () => {
  return <>This is the destination for speaker magic link.</>;
};

export default SpeakerMagicLinkDestination;
